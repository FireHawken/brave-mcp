# `packages/sdk`

Shared internal helper package.

Current contents:

- simulated extension bridge used by daemon and MCP verification

This package should stay thin and focused on integration helpers, not business logic.
