# `packages/protocol`

Shared contract package for the whole stack.

Contents:

- Zod schemas for MCP tool inputs and outputs
- tool catalog metadata
- daemon RPC request and response types
- bridge message schemas
- shared error-code definitions

This package is the source of truth for the public tool surface.
