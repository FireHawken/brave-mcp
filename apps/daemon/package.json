{
  "name": "@brave-mcp/daemon",
  "version": "0.11.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "npm run build -w @brave-mcp/protocol && npm run build -w @brave-mcp/sdk && tsc -p tsconfig.json",
    "verify": "npm run build && node dist/verify.js"
  },
  "dependencies": {
    "@brave-mcp/protocol": "file:../../packages/protocol",
    "@brave-mcp/sdk": "file:../../packages/sdk",
    "fastify": "^5.2.1",
    "pino": "^9.6.0",
    "ws": "^8.18.1",
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "@types/node": "^22.13.14",
    "@types/ws": "^8.5.14",
    "typescript": "^5.8.2"
  }
}
